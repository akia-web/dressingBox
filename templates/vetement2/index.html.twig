{% extends 'base.html.twig' %}

{% block title %}Vetement2 index{% endblock %}

{% block body %}
    <h1>Vetement2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>Couleur</th>
                <th>Taille</th>
                <th>Style</th>
                <th>Marque</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vetement2 in vetement2s %}
            <tr>
                <td>{{ vetement2.id }}</td>
                <td>{{ vetement2.categorie }}</td>
                <td>{{ vetement2.couleur }}</td>
                <td>{{ vetement2.taille }}</td>
                <td>{{ vetement2.style }}</td>
                <td>{{ vetement2.marque }}</td>
                <td>{{ vetement2.photo }}</td>
                <td>
                    <a href="{{ path('vetement2_show', {'id': vetement2.id}) }}">show</a>
                    <a href="{{ path('vetement2_edit', {'id': vetement2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('vetement2_new') }}">Create new</a>
{% endblock %}
